June 15

sqlite> SELECT *
   ...> FROM Invoice
   ...> LIMIT 10;


   -------------------

   sqlite> SELECT Total, BillingCountry
   ...> FROM Invoice;

   -------------------

sqlite> .mode csv
sqlite> .header on
sqlite> .output test01.csv
sqlite> SELECT *
   ...> FROM Invoice;
sqlite> .output stdout
sqlite> .mode list
sqlite> .shell test01.csv
sh: 1: test01.csv: not found

ChatGPT: Yes — .shell works inside the SQLite shell — but not for opening or importing .csv files directly. Instead, it’s used to run external shell commands like ls, cat, or other terminal commands from inside SQLite.

   -------------------

.shell clear /*to clear*/

   -------------------

sqlite> SELECT *
   ...> FROM Invoice
   ...> WHERE Total > 10
   ...> AND InvoiceDate BETWEEN '2013-01-01' AND '2013-06-01';


   -------------------


   SELECT *
   ...> FROM Invoice
   ...> WHERE Total > 10
   ...> AND (InvoiceDate BETWEEN '2009-01-01' AND '2009-03-25'
(x1...> OR InvoiceDate BETWEEN '2010-04-01' AND '2010-04-20');